# TCAD Agent 功能实现与不足说明

## 已实现功能

### 1. 脚本生成与编辑
- 支持通过前端聊天界面生成 TCAD 脚本。
- 可以选择脚本类型：`sdevice`（器件仿真）或 `sprocess`（工艺仿真）。
- 生成脚本时会向后端传递脚本类型参数，后端会使用该参数来生成对应类型的 TCAD 脚本。
- 提供脚本编辑器（`ScriptEditor.tsx`），支持查看、编辑、保存当前脚本。
- 提供示例脚本加载入口，方便用户快速进入编辑状态。

### 2. 聊天与智能交互
- 聊天界面支持用户输入自然语言指令并发送。
- 聊天面板能够识别生成脚本、读取脚本、运行仿真等行为。
- 使用 DeepSeek AI 生成脚本内容并分析用户需求。

### 3. 后端 API 支持
- 提供 `/api/script/read`、`/api/script/write`、`/api/script/parse` 等基本脚本管理接口。
- 提供 `/api/script/generate` 接口，用于按描述生成 TCAD 脚本。
- 新增 `/api/simulation/test` 接口，支持测试当前脚本是否能通过 SSH 连接在虚拟机上执行。
- 支持仿真状态管理接口：`/api/simulation/run`、`/api/simulation/status/:id`、`/api/simulation/stop`、`/api/simulation/reset`。

### 4. SSH 虚拟机执行支持
- 项目支持通过 SSH 连接到虚拟机执行 TCAD 脚本。
- 通过 `virtualMachineTCADService.ts` 实现：
  - SSH 连接与登录
  - 远程目录创建
  - 远程文件传输
  - 远程命令执行
  - 执行Sdevice和Sprocess命令
- 支持读取远程文件、列出远程目录、清理连接等基础操作。

### 5. 脚本验证与清洗
- 对 AI 生成的脚本进行清洗，去除 Markdown 代码块、注释、列表等无效输出。
- 进行基本语法约束验证，确保脚本符合 TCAD 语法要求并避免常见错误。
- 校验 `Grid=` 文件路径、禁止占位符、禁止独立无效关键字等。

### 6. 前端展示与状态管理
- 使用 React + Zustand 管理脚本、聊天消息、仿真状态等全局状态。
- 前端支持多个面板：聊天、脚本编辑、参数设置、仿真控制台、报告查看。
- 使用 Monaco 编辑器提供脚本编辑体验。
- 使用 Chart.js / react-chartjs-2 展示仿真结果图表（在分析服务中生成数据后可展示）。

## 当前不足与待改进点

### 1. AI 脚本生成稳定性
- 目前生成脚本仍可能包含语法错误或不兼容的 TCAD 参数。
- AI 生成的脚本有时会输出无效字段（如 `RecombinationRate`、错误的 `Plot` 条目等），需要进一步增强脚本清洗和约束逻辑。
- `sprocess` 脚本生成规则尚未完全稳定，当前测试中可能会被 TCAD 语法检查拒绝。

### 2. SSH 执行测试覆盖有限
- 当前测试逻辑只检查脚本是否能够在虚拟机上启动执行，但尚未进行完整仿真结果验证。
- 测试结果依赖远程虚拟机环境的配置，与实际 TCAD 文件或网格文件是否存在密切关联。
- 如果远程虚拟机缺少必要网格文件，测试会失败；这部分尚未自动补救。

### 3. 脚本 parser / 参数提取能力
- 目前脚本解析逻辑只做了基本解析，无法保证提取的参数与所有 TCAD 语法完全匹配。
- 对复杂脚本的参数提取和参数修改支持仍较弱。

### 4. 前端 UI 体验
- 尽管已支持脚本编辑和测试按钮，但用户反馈、错误提示和引导仍可以进一步优化。
- 运行日志展示目前仅限基础文本，尚未优化为更友好的可折叠日志或状态条。

### 5. 运行模式与环境依赖
- 项目依赖于 `TCAD_MODE=ssh` 或 `local`，但目前本地运行和虚拟机环境的切换机制没有完整提示。
- 若 SSH 连接失败或 TCAD 环境配置不正确，错误提示仍然比较泛。

### 6. 报告与结果分析
- 报告生成和分析功能基础已搭建，但目前输出结果仍多依赖模拟结果而非真实 TCAD 文件解析。 
- 真实仿真结果解析、结构文件分析与报告生成的稳定性需要进一步验证。

## 建议改进方向

- 增强 AI 脚本生成约束提示，进一步固定输出格式和语法规则。
- 扩展 `virtualMachineTCADService` 的错误分析能力，自动捕获远程执行失败原因并提供更明确的修复建议。
- 加强脚本解析与参数编辑功能，支持更复杂的 TCAD 脚本结构。
- 优化前端测试结果展示，让用户更直观地查看 SSH 执行日志、错误位置和执行状态。
- 补充 `README.md` 或项目文档，明确 `TCAD_MODE`、SSH 配置和虚拟机依赖。
